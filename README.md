# 1주차 과제
## 주제: TDD로 개발하기
## 일정: 2024 년 9월 21일 ~ 27일

## 개요
### 필수 사항
- 테스트 케이스의 작성 및 작성 이유를 주석으로 작성
- 설정 및 database패키지 구현체 수정 없이 활용하여 기능 구현
- 분산환경 고려하지 않음

### 요건
포인트 조회, 포인트 충전/사용 내역 조회, 충전, 사용
```
- PATCH  `/point/{id}/charge` : 포인트를 충전한다.
- PATCH `/point/{id}/use` : 포인트를 사용한다.
- GET `/point/{id}` : 포인트를 조회한다.
- GET `/point/{id}/histories` : 포인트 내역을 조회한다.
```
- 잔고가 부족할 경우, 포인트 사용은 실패
- 동시에 여러 건의 포인트 충전, 이용 요청이 들어올 경우 순차적으로 처리되어야 함

### pass 조건
lv.0
- `/point` 패키지 (디렉토리) 내에 `PointService` 기본 기능 작성
- `/database` 패키지의 구현체는 수정하지 않고, 이를 활용해 기능을 구현
- 각 기능에 대한 단위 테스트 작성

lv.1
- 포인트 충전, 사용에 대한 정책 추가 (잔고 부족, 최대 잔고 등)
- 동시에 여러 요청이 들어오더라도 순서대로 (혹은 한번에 하나의 요청씩만) 제어될 수 있도록 리팩토링
- 동시성 제어에 대한 통합 테스트 작성
    
lv.2
- 동시성 제어 방식에 대한 분석 및 보고서 작성 ( **README.md** )

---
# 작업 기록

## 요건 분석 및 설계
### 테이블
- UserPointTable

|컬럼 명|타입|설명|
|---|---|---|
|id|long|사용자 식별 id|
|point|long|해당 사용자의 잔여 point|
|updateMillis|long|업데이트 일자(밀리초 단위, 타임스탬프)|

- PointHistoryTable

|컬럼 명|타입|설명|
|---|---|---|
|id|long|트랜잭션 식별 id|
|userId|long|사용자 식별 id|
|amount|long|포인트 변경 금액|
|type|TransactionType|트랜잭션 유형 (CHARGE, USE)|
|updateMillis|long|업데이트 일자(밀리초 단위, 타임스탬프)|

### 포인트 조회
1. 파라미터 수신: 사용자가 조회할 포인트 관련 정보를 담은 파라미터를 넘겨받는다. 
2. 파라미터 검증: 수신한 파라미터의 유효성을 검사하여 필수 값이 포함되어 있는지, 형식이 올바른지 확인한다.
3. 데이터 요청: 검증된 파라미터를 기반으로 UserPointTable 데이터를 요청한다.
4. 결과 반환: 최종적으로 조회된 포인트 데이터를 사용자에게 반환한다.

|Test case 번호|type|설명|validation|HTTP 상태 코드|
|---|---|-----|-----|---|
|1|실패|id 형식 타입이 long이 아닌 경우|잘못된 요청입니다. ID는 숫자 형식이어야 합니다.|400|
|2|실패|id가 0 또는 음수 일 경우|ID는 1 이상의 정수여야 합니다|400|
|3|실패|조회 결과가 없는 경우|조회 결과가 없습니다.|404|
|4|성공|조회 결과가 하나 이상 있는 경우 (리스트 반환)|-|200|

### 포인트 충전/사용 내역 조회
1. 파라미터 수신: 사용자가 조회할 포인트 관련 정보를 담은 파라미터를 넘겨받는다. 
2. 파라미터 검증: 수신한 파라미터의 유효성을 검사하여 필수 값이 포함되어 있는지, 형식이 올바른지 확인한다.
3. 데이터 요청: 검증된 파라미터를 기반으로 PointHistoryTable 데이터를 요청한다.
4. 결과 반환: 최종적으로 조회된 포인트 사용/충전 내역 데이터를 사용자에게 반환한다.

|Test case 번호|type|설명|validation|HTTP 상태 코드|
|---|---|-----|-----|---|
|1|실패|id 형식 타입이 long이 아닌 경우|잘못된 요청입니다. ID는 숫자 형식이어야 합니다.|400|
|2|실패|id가 0 또는 음수 일 경우|ID는 1 이상의 정수여야 합니다|400|
|3|실패|조회 결과가 없는 경우|조회 결과가 없습니다.|404|
|4|성공|조회 결과가 하나 이상 있는 경우 (리스트 반환)|-|200|

### 포인트 충전
1. 파라미터 수신: 사용자가 조회할 포인트 관련 정보를 담은 파라미터를 넘겨받는다. 
2. 파라미터 검증: 수신한 파라미터의 유효성을 검사하여 필수 값이 포함되어 있는지, 형식이 올바른지 확인한다.
3. 비즈니스 로직 수행: 검증된 파라미터를 기반으로 UserPointTable애서 해당 사용자의 포인트를 조회한다. amount만큼 포인트를 증가시킨다.
4. 통계 저장: 포인트 충전 내역을 PointHistoryTable에 저장한다.
5. 결과 반환: 최종적으로 업데이트된 사용자 포인트 정보를 포함하여 응답을 반환한다.

|Test case 번호|type|설명|validation|HTTP 상태 코드|
|---|---|-----|-----|---|
|1|실패|id 형식 타입이 long이 아닌 경우|ID는 숫자 형식이어야 합니다.|400|
|2|실패|id가 0 또는 음수 일 경우|ID는 1 이상의 정수여야 합니다.|400|
|3|실패|amount 형식 타입이 long이 아닌 경우|금액은 숫자 형식이어야 합니다.|400|
|4|실패|amount가 0 또는 음수 일 경우|금액은 1 이상의 정수여야 합니다.|400|
|5|실패|존재하지 않는 id로 요청 시 사용자 정보를 찾을 수 없는 경우|사용자를 찾을 수 없습니다.|404|
|6|성공|유효한 id와 amount로 포인트 충전 시 성공적으로 업데이트되는 경우|-|200|
|7|성공|충전 후 업데이트된 포인트 정보가 올바른 경우|-|200|
|8|성공|포인트 충전 내역이 PointHistoryTable에 올바르게 저장되는 경우|-|201|

### 포인트 사용
1. 파라미터 수신: 사용자가 조회할 포인트 관련 정보를 담은 파라미터를 넘겨받는다. 
2. 파라미터 검증: 수신한 파라미터의 유효성을 검사하여 필수 값이 포함되어 있는지, 형식이 올바른지 확인한다.
3. 비즈니스 로직 수행: 검증된 파라미터를 기반으로 UserPointTable애서 해당 사용자의 포인트를 조회한다. amount만큼 포인트를 차감시킨다.
4. 통계 저장: 포인트 충전 내역을 PointHistoryTable에 저장한다.
5. 결과 반환: 최종적으로 업데이트된 사용자 포인트 정보를 포함하여 응답을 반환한다.

|Test case 번호|type|설명|validation|HTTP 상태 코드|
|---|---|-----|-----|---|
|1|실패|id 형식 타입이 long이 아닌 경우|ID는 숫자 형식이어야 합니다.|400|
|2|실패|id가 0 또는 음수 일 경우|ID는 1 이상의 정수여야 합니다.|400|
|3|실패|amount 형식 타입이 long이 아닌 경우|금액은 숫자 형식이어야 합니다.|400|
|4|실패|amount가 0 또는 음수 일 경우|금액은 1 이상의 정수여야 합니다.|400|
|5|실패|존재하지 않는 id로 요청 시 사용자 정보를 찾을 수 없는 경우|사용자를 찾을 수 없습니다.|404|
|6|실패|UserPointTable에 저장된 포인트보다 많은 양의 포인트사용을 요청하는 경우|사용할 수 있는 포인트가 부족합니다.|400|
|7|성공|유효한 id와 amount로 포인트 차감 시 성공적으로 업데이트되는 경우|-|200|
|8|성공|차감 후 업데이트된 포인트 정보가 올바른 경우|-|200|
|9|성공|포인트 차감 내역이 PointHistoryTable에 올바르게 저장되는 경우|-|201|



